# تعليمات المنشور اليومي

هذا الملف هو المرجع المعتمد لكيفية إنتاج كاروسيل إنستقرام اليومي لحساب
**@al_t506**. اقرأه كاملاً قبل التصميم.

---

## المسار الحالي: ريلز تعليمي فقط، كل يومين

**ألغيت الأخبار والكاروسيل** بطلب صاحب الحساب في ٢٠٢٦-٠٨-٢٩. الكاروسيل
الإخباري كان يجني ١–٣ إعجابات ويشتّت هوية الحساب بين مسارين.

- **النوع الوحيد:** ريلز تعليمي بـ `render_reel3.py`.
- **الإيقاع:** كل يومين. `preflight` يحسب الفارق من آخر `date` في
  `posts.json` — **إن كان أقل من يومين فلا تنشر**، أنهِ بسطر واحد وتوقّف.
- **الموضوع:** تجربة عملية مع **وكيل ذكاء اصطناعي أو أداة** — ما طلبتَه
  منه، وما فعله، وما خرج فعلاً. تجربةٌ لا وصفاً، ونتيجةٌ لا وعداً.

### اللقطات الحقيقية — قلب المسار الجديد

`demo.type = "shots"` يعرض صوراً حقيقية بمونتاج: كل لقطة تدخل على ضربة،
وتتحرّك ببطء وهي معروضة (كين بيرنز) فلا يجمد الكادر، ويمكن وضع خاتم يشير
إلى نقطة فيها.

```json
"demo": {
  "type": "shots", "label": "التجربة كما جرت",
  "frame": "browser",            // browser · phone · none
  "url": "ollama.com",           // يظهر في شريط المتصفح
  "shots": [
    {"src": "shots/1.png", "note": "هنا يظهر النموذج", "focus": [0.76, 0.24]},
    {"src": "shots/2.png", "note": "الجواب خرج بلا اتصال"}
  ]
}
```

`focus` إحداثيات **نسبية** (٠–١) من أعلى يسار الصورة. بوجودها يظهر خاتم
نابض مع سطر الشرح عند تلك النقطة؛ بدونها يظهر `note` تعليقاً تحت اللقطة.

**من أين تأتي اللقطات؟** بالترتيب:

1. **من صاحب الحساب** — يرسل لقطات تجربته. هذا الأصدق: تجربته هو.
2. **مخرجات حقيقية تُلتقط هنا** — شغّل الأمر فعلاً واحفظ مخرجاته.
3. **أصول رسمية من مستودعات الأدوات على GitHub** — شعارات ولقطات
   المنتج. GitHub وحده يمرّ عبر الوسيط.

**ما لا يصلح:** لقطات لمواقع الأدوات ملتقطة هنا — Chromium محجوب عنها
(`ERR_TUNNEL_CONNECTION_FAILED`). ولا تقدّم إعادة تمثيل على أنها لقطة
حقيقية؛ إن لم تتوفّر لقطة فاستعمل `chat` أو `table` وهما إعادة تمثيل
صريحة الشكل.

---

## ~~نوعان يتناوبان يوماً بيوم~~ (ملغى — للرجوع فقط)

الحساب ينشر نوعين بالتناوب. `tools/preflight.py` يحدد نوع اليوم تلقائياً من
آخر منشور في `posts.json` ويطبعه — التزم بما يطبعه.

| النوع | المحتوى | بنية الشرائح |
|---|---|---|
| `news` | خبر تقنية أو ذكاء اصطناعي من آخر ٢٤–٤٨ ساعة | `cover ← point ← stat ← point ← cta` |
| `guide` | كيف يستفيد شخص أو مؤسسة من الذكاء الاصطناعي عملياً | `cover ← point 1 ← point 2 ← point 3 ← cta` |

سجّل `"kind": "news"` أو `"kind": "guide"` في `posts.json` بعد النشر — عليه
يعتمد تناوب الغد.

### النوع التعليمي (`guide`)

الهدف أن يخرج القارئ وقد تعلّم شيئاً يطبّقه اليوم، لا أن يُعجَب بالتقنية.

**اختر جمهوراً واحداً محدداً** لكل منشور — لا «الجميع». مثلاً: صاحب متجر
صغير · معلّم · محاسب · مسوّق · موظف موارد بشرية · طالب جامعي · صاحب مطعم ·
مصمم · كاتب محتوى · صيدلي · مالك عيادة.

**ابدأ بألم يعرفه القارئ**، لا بقدرات النموذج:
- ❌ «الذكاء الاصطناعي يستطيع تحليل البيانات»
- ✅ «تقضي ساعتين كل خميس في تقرير المبيعات؟ إليك كيف تختصرها إلى عشر دقائق»

**الشرائح الثلاث الوسطى = ثلاث خطوات أو استخدامات ملموسة.** كل واحدة:
- فعل محدد يستطيع القارئ تنفيذه، لا فكرة عامة
- اذكر الأداة بالاسم حين يفيد (ChatGPT · Claude · Gemini · NotebookLM · Excel Copilot)
- إن كان المفيد موجّهاً (prompt)، اكتبه فعلاً بصيغة جاهزة للنسخ

**تجنّب:** الوعود المبالغ فيها · «سيغيّر حياتك» · قوائم أدوات بلا سياق ·
الحديث عن المستقبل بدل الآن.

**الكابشن** في المنشور التعليمي أهم من الخبري: وسّع الخطوات الثلاث بأمثلة،
واذكر تحذيراً واحداً صادقاً (مراجعة المخرجات · خصوصية بيانات العملاء ·
لا تعتمد على النموذج في الأرقام النهائية).

**تفادي التكرار:** راجع `posts.json` — لا تكرّر الجمهور نفسه ولا الاستخدام
نفسه من آخر خمسة منشورات تعليمية.

---

### الرسوم الدلالية

أضف `"art"` إلى أي شريحة ليظهر رسم متجه بلون الثيم في المساحة الفارغة.
الرسم **يدلّ على المعنى ولا يزيّن فقط** — اختره من الموضوع، ولا تضعه في كل
شريحة (شريحتان إلى ثلاث من الخمس تكفي؛ الازدحام يضعف الأثر).

| الرسم | يدلّ على |
|---|---|
| `network` | النماذج · الشبكات · الوكلاء · الاتصال |
| `waves` | البحر · التبريد · البنية التحتية المائية |
| `document` | النصوص · الكتابة · التوثيق · العلامات |
| `chart` | النمو · الأرقام · المقارنات · التقارير |
| `device` | التشغيل المحلي · الأجهزة · التطبيقات |
| `steps` | الخطوات · الإجراءات · المحتوى التعليمي |
| `shield` | الأمان · الخصوصية · الحوكمة · المراجعة |
| `clock` | توفير الوقت · الكفاءة · الأتمتة |

```json
{"type":"point","n":1,"title":"...","body":"...","art":"chart"}
```

اسم غير معروف يطبع تحذيراً ويُتجاهل — لن يكسر التوليد.

**الحركة غير ممكنة** في كاروسيل الصور؛ إنستقرام يعرضه ثابتاً. الحركة تحتاج
ريلز (فيديو) وهي صيغة أخرى غير مستخدمة حالياً.

---

### الريلز — النوع التعليمي

المنشور التعليمي يخرج **فيديو رأسياً** لا كاروسيل. المولّد:

```bash
python3 tools/render_reel3.py reel.json out.mp4 --stills 0,5,11,15,19  # اضبط التصميم
python3 tools/render_reel3.py reel.json out.mp4                        # ثم رمّز (~٤ دقائق)
```

**استخدم `render_reel3.py`.** المولّدان `render_reel.py` و `render_reel2.py`
متروكان للرجوع فقط ولا يُستعملان. الثاني كان كاميرا واحدة تنزلق فوق بطاقات
نصية لثلاثين ثانية: بلا قطع ولا حركة داخل الكادر ولا إيقاع، فانهار زمن
المشاهدة وانهار معه التوزيع.

**اضبط باللقطات أولاً دائماً.** الترميز الكامل يستغرق ~٤ دقائق، واللقطات ثوانٍ.

#### مبادئ المونتاج — هي المنتج

**بنية المشاهد:** `cover ← demo ← value ← prompt ← cta` — نحو ٢٠ ثانية
(٣٢ نبضة: ٣ + ١١ + ٧ + ٦ + ٥).

- **المدة مضاعف صحيح للنبضة.** المولّد يقرأ إيقاع الموسيقى من
  `reel_audio3.grid()` ويحسب المشاهد عليه، فتقع كل قطعة على ضربة وتُعاد
  الحلقة على الإيقاع بلا خياطة مسموعة. لا تكتب مدداً بالثواني يدوياً.
- **كل قطعة مُصوَّتة**: صعود يمهّد قبلها بـ٠٫٤ث، نقرة عند اللحظة، ضربة
  باص تعطيها ثقلاً. تُمرَّر مواضع القطع تلقائياً إلى `write_bed`.
- **الطاقة تُبنى ولا تبدأ كاملة**: القسم الأول وسادة وباص، ثم يدخل
  الأربيجيو والطبلة، ثم الهاي هات، ثم اللمعة العليا. المستمع يُحسّ أن
  الشيء يكبر فيبقى.
- **الإطار صفر غلافٌ مكتمل.** مشهد `cover` لا يُبنى من العدم — كل شيء
  ظاهر عند الثانية صفر والحركة استقرارٌ لا ظهور. إنستقرام يلتقط الغلاف
  من الفيديو، وأي إطار افتتاحي ناقص يصير مستطيلاً فارغاً في الشبكة.
- **حركة داخل الكادر لا حركة كاميرا فقط**: كتابة تُطبع، صفوف تهبط،
  أعمدة تنمو. الكادر الساكن يُفقد المشاهد في ثانيتين.

**المنطقة الآمنة:** لا شيء مهم تحت `y=1580` ولا يمين `x=980` — هناك تجلس
واجهة إنستقرام (التعليق والاسم والأزرار).

#### المشاهد الخمسة

`cover` — `kicker` · `title` · `result` (سطر أو سطران يطلّان في بطاقة).

`demo` — **المحرّك**. `type` واحد من:
- `chat` — سؤال يُطبع حرفاً حرفاً ثم نقاط انتظار ثم جواب يتدفّق كلمةً كلمة.
  الحقول: `you` · `reply[]` · `status`.
- `table` — جدول يُبنى صفاً صفاً ثم يهبط المجموع. الحقول: `head[]` ·
  `rows[][]` · `total[]`.
- `bars` — أعمدة مقارنة تنمو. الحقل: `bars[]` بـ`label` و `pct` و `value`.

`value` — **الفائدة صريحةً**، ثلاثة أسطر تهبط على النبضة، كلٌّ بـ`big`
(رقم أو رمز) و `title` و `sub`. لا تترك الفائدة ضمنيةً في العرض: هنا
يقرّر المشاهد أن الأمر يستحق وقته.

`prompt` — **إلزامي**. موجّه أو أمر جاهز للنسخ. إن خلت كل الأسطر من
العربية عُرضت في **نافذة طرفية** حقيقية بشريط علوي وأمر أخضر ومخرجات
(`out[]`) تنزل سطراً كل نصف نبضة — أقوى بصرياً وأصدق تقنياً. وإلا فبطاقة
باللون المميّز. **لا تكتب عربية في `out`**: الخط الأحادي لا يشكّلها.

`cta` — تركيب مركزيّ: صورة صاحب الحساب في قرص كبير بإطار، ثم `name`
تحتها، ثم `title`، ثم زر الكلمة المفتاحية، ثم `sub`. الصورة من
`assets/avatar.png` في المستودع.

#### الجمهور: تقني، غير متخصص

الريلز موجّه لمهتمّ بالتقنية والذكاء الاصطناعي، لا لمبتدئ تماماً ولا
لمهندس. ارفع السقف عن الشرح المدرسي، وابقَ دون التعمّق البرمجي.

- **الأدوات بالاسم والأمر الحقيقي.** `ollama run qwen2.5` أفضل من «حمّل
  التطبيق». الدقة التقنية نفسها هي المحتوى.
- **ابدأ من الألم لا من التقنية.** «ملفاتك لا تغادر جهازك» يفهمها الجميع؛
  «استدلال محلي» لا تعني شيئاً.
- **اجعل النتيجة مرئية في أول ثانيتين.** المشاهد يقرر البقاء قبل أن يفهم.
- **أداة واحدة لكل ريلز**، مجانية أو بنسخة مجانية.
- **اذكر الشرط الصادق** إن وُجد (ذاكرة مطلوبة، حساب، حدّ استعمال). إخفاؤه
  يكسب مشاهدة ويخسر ثقة.

#### الكلمة المفتاحية في مشهد الدعوة

كل ريلز تعليمي ينتهي بطلب واضح: أن يكتب المشاهد **كلمة واحدة** في
التعليقات ليصله رابط الأداة وطريقة استخدامها.

```json
"keyword": "أداة",
"cta": {"name":"@al_t506",
        "title":"اكتب «أداة» وأرد عليك",
        "sub":"أرد على تعليقك برابط الأداة المجانية وخطوات تشغيلها."}
```

`keyword` في جذر الملف لا داخل `cta`. تظهر الكلمة في زر بارز تحت العنوان
مباشرةً — أوضح عنصر في المشهد بعد الصورة.

**عِد بما نستطيع الوفاء به: رد تحت التعليق، لا رسالة خاصة.** لا نملك قدرة
إرسال الرسائل الخاصة (`/conversations` يرجع «Application does not have the
capability»)، وأتمتة ManyChat المجانية لا تغطي كل المنشورات. صياغة مثل
«سيصلك» أو «أرسل لك» توحي برسالة خاصة لن تصل، فتفقد ثقة من علّق. اكتب
دائماً «أرد عليك… تحت تعليقك».

**الكلمة ثابتة: «أداة»** — لا تغيّرها. سجّلها في `posts.json` بحقل `keyword`
مع نص الرد في `reply` — عليهما تعتمد مهمة الرد على المعلّقين.

**الأداة ورابطها يذهبان في حقل `reply`** — وهو نص الرد العلني الذي يصل
للمعلّقين. اكتبه كاملاً: اسم الأداة، رابطها، وخطوة أو خطوتان للاستخدام.

**الرابط لا يظهر على الشاشة عمداً.** إنستقرام لا يجعل النص في الفيديو
قابلاً للنقر، فرابط مكتوب في الكادر يعني أن يحفظه المشاهد يدوياً أو
يخطئ في نسخه — وكلاهما يفقدك التفاعل. الرابط يصل في الرد على تعليقه،
وهو نفسه ما يجعل التعليق يستحق أن يُكتب. لذلك:

- في ملف الريلز ضع `tool` (اسم · رابط) و `reply` (نص الرد كاملاً).
- بعد النشر انقلهما إلى `posts.json` مع `keyword`.
- بلا `reply` مكتوبٍ ومكتفٍ بذاته، الريلز يَعِد بشيء لا يصل — لا تنشره.

---

## الرد على المعلّقين

**قناة واحدة: الرد العلني تحت التعليق**، تنفّذه مهمة «متابعة التعليقات» كل
٣ ساعات، ويحمل أداة اليوم ورابطها من حقل `reply` في `posts.json`.

**ManyChat خارج المسار — لا تبنِ عليها.** جُرّبت الباقة المجانية فتبيّن أنها
لا تكفي من وجهين: استهداف «كل المنشورات» ميزة في باقة Essential المدفوعة،
والمحتوى المتغيّر (External Request) في Pro. يبقى المتاح مجاناً أتمتة
مربوطة بمنشور واحد ترسل نصاً ثابتاً — أي عمل يدوي كل يوم مقابل رسالة لا
تحمل الرابط أصلاً. لذلك `tools/manychat_payload.py` و `manychat/latest.json`
معطّلان ولا يُستدعيان.

**الرد العلني أقوى من الرسالة الخاصة هنا، لا أضعف:** يحمل رابط أداة ذلك
الريلز تحديداً، ويعمل على كل المنشورات بلا سقف شهري، ويراه بقية المشاهدين
فيشجّعهم على التعليق. الشرط الوحيد أن يكون `reply` مكتفياً بذاته.

**كل منشور له أداته.** `pending_replies.py` يبحث عن المنشور بـ `media_id`
ثم يقرأ `keyword` و `reply` **من ذلك المنشور نفسه**. من يعلّق على ريلز
الصور القديمة يصله رابط أداة ترميم الصور، ومن يعلّق على ريلز نبرة الردود
يصله رابط أداة أخرى — ولو كتب الاثنان الكلمة نفسها. الكاروسيل الخبري بلا
`keyword` فيُستبعد أصلاً.

**النافذة:** `watch_targets.py` يختار كل منشور يحمل `keyword` و `reply`
ونُشر خلال آخر ٣٠ يوماً (بحد أقصى ١٠). لا تكتفِ بالأحدث: الريلز يجمع
تعليقات لأسابيع، ومن يعلّق بعد أسبوع فعل تحديداً ما طلبه الفيديو منه.

**آلية الرد العلني:**

1. اجلب تعليقات آخر منشور:
   `GET /v21.0/<media_id>/comments?fields=id,text,username,timestamp&limit=50`
2. مرّرها للمنطق: `python3 tools/pending_replies.py <media_id> < comments.json`
3. لكل عنصر في `actions`:
   `POST /v21.0/<comment_id>/replies?message=<message>`
4. أضف معرّفات التعليقات المُجابة إلى `state/replied.json` وادفعها.

**الخطوة الرابعة إلزامية** — بدونها يتكرر الرد على نفس الشخص بعد إعادة بناء
الحاوية. المطابقة تتجاهل التشكيل والتطويل وتعامل «ة/ه» و«ى/ي» سواء، فلا
تشترط كتابة دقيقة من المشاهد.

```json
{"kind":"prompt","dur":6.2,"label":"الموجّه","title":"...",
 "hint":"التقط الشاشة — الموجّه جاهز للنسخ",
 "lines":["السطر الأول.","السطر الثاني.","السطر الثالث."]}
```

**أساليب الرحلة** (حقل `style` في جذر الملف) — اختر مختلفاً عن آخر ريلز،
و`preflight` يعرض المتاح. الأسلوب يغيّر شكل الطيران نفسه لا اللون:

| الأسلوب | مسار الكاميرا | الأرضية | المسار المرسوم |
|---|---|---|---|
| `flight` | تعرّج قُطري بين المحطات | شبكة نقاط | نعم |
| `column` | هبوط عمودي مستقيم | مسطرة أفقية | لا |
| `tilt` | تعرّج واسع مع ميل خفيف للوحة | شبك مائل متقاطع | نعم |

**الثيم:** `paper` (أرضية فاتحة) هو الافتراضي للريلز — يفصل الفيديو بصرياً
عن الكاروسيل الداكن في شبكة الحساب. استخدم ثيماً داكناً كل ثالث ريلز.

**الخطوط:** الريلز يستعمل خطّين لا يستعملهما الكاروسيل، فتختلف نبرته:
`Readex Pro` للعناوين و`IBM Plex Sans Arabic` للمتن، وكلاهما في `fonts/`
بجذر المستودع. لا تعُد إلى Cairo/Tajawal في الريلز — هما شبكة أمان فقط.

**حدود النص:** عنوان الهوك ≤ ٨ كلمات · عنوان الخطوة ≤ ٦ · النص ≤ ٢٨ كلمة ·
سطر الموجّه ≤ ١٢ كلمة.

**التحقق البصري:** التقط لقطات عند منتصف كل مشهد. الكاميرا تطير في آخر
٠٫٩ ثانية قبل بداية كل مشهد، فلقطة في تلك النافذة تُظهر مشهدين معاً باهتين
— وهذا سليم، لكنه لا يصلح لفحص التخطيط.

**الصوت** يُركَّب تلقائياً مع كل ريلز: مقطوعة حقيقية بدورة كوردات وباص
وأربيجيو جرسي وطبقة عليا لامعة — ممتعة تُسمع بذاتها، وبلا إيقاع صاخب وبلا
**الموسيقى: مقاطع حقيقية بترخيص CC0-1.0 في `music/`** — لا مركّبة. لا حاجة
لأي إعداد، لكن افهم الترتيب لأنه معكوس عمّا يُتوقَّع: `reel_music.analyse()`
يستخرج **إيقاع المقطع وطَور ضربته** أولاً، ثم يبني `plan_beats()` مدد
المشاهد عليه. الموسيقى تحكم المونتاج لا العكس، فتقع كل قطعة على ضربة
حقيقية في الأغنية.

ويختار `analyse()` كذلك **بداية المقطع** من بذرة المحتوى — بعد المقدّمة،
على ضربة، ومن النصف الأعلى طاقةً — فيسمع المتابع جزءاً مختلفاً من الأغنية
نفسها كل منشور: هوية صوتية واحدة بلا تكرار حرفيّ.

فوق الموسيقى يضع `build_bed()` تصميم القطعات (صعود · نقرة · ضربة باص)،
ويكبح الموسيقى ٢٨٪ لحظة كل قطعة لتُسمع الضربة. التراخيص في `music/README.md`
— **لا تُضف مقطعاً بلا ترخيص يسمح بالاستعمال التجاري.**

إن كان `music/` فارغاً يرجع المولّد تلقائياً إلى `reel_audio3.py` المركّبة.

**الرفع:** ارفع `reel.mp4` إلى مجلد `<التاريخ>-reel` على `claude/posts`.

### نشر الكاروسيل — انتبه لمفتاح الإجراء

```
tool_name: instagram_for_business_publish_photo_s
action:    publish_media_v2          ← وليس publish_photo_s
media:     الروابط الخمسة بالترتيب
instagramPageId: 17841402194299475
```

**`publish_photo_s` هو `tool_name` لا `action`.** تمريره في حقل `action`
يرجع `Action 'publish_photo_s' not found`. حدث هذا فعلاً في نشر
2026-08-29. عند أي شكّ استدعِ `inspect_zapier_actions` بـ `tool_name`
واقرأ `key` من الاستجابة.

### نشر الريلز — عبر Graph API مباشرة، لا عبر أداة Zapier الجاهزة

**لا تستعمل `instagram_for_business_publish_video`.** فحصنا مخطّطها: حقولها
`video · caption · location · tagged_users · instagramPageId` — **لا حقل
غلاف فيها**. فيلتقط إنستقرام إطاراً من الفيديو باجتهاده، وقد يقع على لقطة
خلفية فارغة. هذا سبب ظهور أربعة من ريلزات الحساب مستطيلاتٍ صمّاء في الشبكة.

الطريق الصحيح ثلاث نداءات على `instagram_for_business_make_api_mutating_request`
و `instagram_for_business_make_api_get_request`، ومعرّف الحساب `17841402194299475`:

**١) أنشئ الحاوية**

    url: https://graph.facebook.com/v21.0/17841402194299475/media
    method: POST
    querystring: {"media_type":"REELS",
                  "video_url":"<رابط raw>",
                  "caption":"<الوصف>",
                  "thumb_offset":"0",
                  "share_to_feed":"true"}

يعيد `id` — معرّف الحاوية، وليس معرّف المنشور.

`thumb_offset=0` هو بيت القصيد: يجعل **الإطار الأول** هو الغلاف قطعاً لا
احتمالاً. ولهذا يبني `render_reel3.py` الإطار صفر غلافاً مكتملاً.

**٢) انتظر انتهاء التحويل**

    url: https://graph.facebook.com/v21.0/<معرّف الحاوية>
    querystring: {"fields":"status_code,status"}

كرّر حتى `status_code` = `FINISHED` (دقيقة أو دقيقتان عادةً). إن صار
`ERROR` فاقرأ `status` وأبلغ ولا تنشر.

هذه الخطوة هي ما يلغي خطأ «Video is still processing» من أصله: الأداة
الجاهزة كانت تنشئ الحاوية وتنشر فوراً في نداء واحد، فتصطدم بالتحويل.

**٣) انشر**

    url: https://graph.facebook.com/v21.0/17841402194299475/media_publish
    method: POST
    querystring: {"creation_id":"<معرّف الحاوية>"}

يعيد `id` المنشور — تحقّق منه كما في قسم «تأكيد النشر».

**إن استعملت الأداة الجاهزة رغم ما سبق** — أو ظهر الخطأ من طريق آخر:

    Video is still processing. In Progress: Media is still being processed.

**هذا ليس رفضاً** — الملف وصل وبدأ التحويل. الإجراء الصحيح:

1. لا تغيّر الرابط ولا تعِد الترميز — لا خلل فيهما.
2. تحقّق أولاً أن المنشور لم يُنشر فعلاً:
   `GET https://graph.facebook.com/v21.0/17841402194299475/media` مع
   `fields=id,media_type,media_product_type,permalink,timestamp&limit=3`
3. إن لم يظهر ريلز جديد، أعد استدعاء النشر. الحاويات غير المنشورة تنتهي
   تلقائياً فلا خطر منها، لكن النشر مرتين ينتج منشورين — لذلك التحقق
   قبل كل إعادة محاولة **إلزامي**.
4. أعد المحاولة حتى ثلاث مرات مع تباعد. إن فشلت الثالثة، أبلغ ولا تنشر.

إن فشلت ثلاث محاولات، **افحص معدل بت الفيديو** قبل أن تستسلم:

    ffmpeg -i reel.mp4   # ابحث عن سطر Video: ... kb/s

التصاميم المسطّحة (أسلوب `poster` خاصة) تُضغط إلى أقل من 400 كيلوبت/ث، وعندها
لا ينهي إنستقرام التحويل أبداً. المولّد يفرض الآن 6 ميغابت/ث فلا يتكرر هذا.

النجاح = `media_product_type` يساوي `REELS`.

---

## القاعدة الأهم: التنويع البصري

**استخدم `tools/render_v2.py` — وليس `render.py` الموجود مع المهارة.**

```bash
python3 tools/render_v2.py slides.json ./out
```

المولّد المرفق مع المهارة يمرّر كل غلاف عبر قالب واحد: شارة، رقم لاتيني ضخم،
عنوان، سطر فرعي. النتيجة أن كل المنشورات تبدو متطابقة في شبكة الحساب المصغّرة،
ويتغيّر اللون وحده. هذا ما نتجنّبه.

`render_v2.py` يقدّم خمسة تراكيب مختلفة للغلاف، لكل واحد خلفية وتوقيع بصري
مختلفان — والفرق يُرى عند حجم الإبهام، وهو ما يهم فعلاً.

### تخطيطات الغلاف

يُحدَّد بحقل `layout` داخل شريحة `cover`.

| التخطيط | متى يُستخدم | الحقول |
|---|---|---|
| `numeral` | الرقم هو القصة | `big` `title` `sub` `kicker` |
| `manchette` | خبر بلا رقم مميز — العنوان هو الصورة | `title` `sub` `kicker` `big` (وسم صغير) |
| `stencil` | يجتمع رقم وخبر — الرقم مفرّغ خلف العنوان | `big` `title` `sub` `kicker` |
| `band` | عاجل أو خبر حاد — أعلى تباين في الشبكة | `title` `big` `sub` `kicker` |
| `ledger` | مقارنة أو أرقام متعددة | `title` `rows` `kicker` |

`ledger` يحتاج:

```json
"rows": [
  {"value": "30B",  "label": "عدد المعاملات"},
  {"value": "24GB", "label": "الذاكرة المطلوبة"}
]
```

ثلاثة صفوف حد أقصى، وإلا تلامس الخطوط التذييل.

### الثيمات

`indigo` تقنية · `emerald` نمو ومال · `crimson` عاجل · `amber` أجهزة وطاقة ·
`violet` إبداع ومستقبل · `steel` رمادي بذهبي بنبرة تحريرية ·
**`paper` أرضية فاتحة**

`paper` هو أقوى كاسر للرتابة لأن بقية المنشورات داكنة. استخدمه كل ٣–٤ منشورات.

### قاعدة الاختيار

اقرأ `posts.json` وانظر `layout` و `theme` لآخر **٣** منشورات، واختر مختلفاً عنهما.
`tools/preflight.py` يطبع لك القائمتين المتاحتين تلقائياً عند تشغيله.

---

## بنية الشرائح

خمس شرائح بالضبط: `cover ← point ← stat ← point ← cta`

```json
{
  "theme": "steel",
  "handle": "@al_t506",
  "slides": [
    {"type":"cover","layout":"manchette","kicker":"تحليل",
     "title":"...","sub":"...","big":"Meta"},
    {"type":"point","n":1,"title":"ماذا حدث؟","body":"..."},
    {"type":"stat","big":"150M","title":"...","body":"..."},
    {"type":"point","n":2,"title":"لماذا يهمك؟","body":"..."},
    {"type":"cta","title":"...","body":"...","name":"@al_t506"}
  ]
}
```

شريحة `cta` تعرض الصورة الشخصية تلقائياً عند وجود `name`.

### حدود الطول

`title` ≤ ٧ كلمات · `body` ≤ ٣٥ كلمة · `sub` ≤ ١٢ كلمة

في `manchette` و `band` يتحمّل العنوان ٨ كلمات لأن الخط أكبر والتخطيط أوسع.

تجاوز هذه الحدود يكسر التخطيط — والتحقق البصري سيكشفه، لكن الأفضل عدم الوصول
إلى ذلك.

---

## التحقق البصري — خطوة إلزامية

اقرأ **كل** صورة مولّدة بأداة Read وافحصها فعلياً:

- الحروف العربية متصلة وغير مقلوبة
- لا نص مقصوص أو خارج الإطار
- لا تداخل بين المحتوى والتذييل
- النص اللاتيني داخل العربية (أسماء المنتجات) في موضعه الصحيح

أصلح وأعد التوليد عند أي خلل. لا تسلّم صوراً لم تنظر إليها.

---

## الكتابة

المرجع: `references/copywriting.md` في مجلد المهارة. خلاصته:

- الهوك في أول سطرين يقرر إن كان أحد سيكمل القراءة
- عربية فصحى مبسّطة، جمل قصيرة، كل جملة فكرة واحدة
- خاطب القارئ بـ«أنت»
- الأرقام لاتينية داخل النص العربي
- شريحة «لماذا يهمك» هي الأهم — بدونها المنشور مجرد نقل خبر
- تجنّب البدايات الميتة: «تشير التقارير إلى…» · «في خبر مثير…»

الكابشن: هوك سطران، شرح ٣–٥ أسطر بفقرات قصيرة، سؤال مفتوح، ثم ٥–٨ هاشتاقات
عربية وإنجليزية. الحد ٢٢٠٠ حرف.

---

## بعد النشر

سجّل المنشور في `posts.json` بحقول: `date` · `topic` · `keywords` · `theme` ·
`layout` · `permalink` · `media_id` · `sources`.

حقل `layout` هو ما يمنع تكرار التصميم غداً. لا تتجاهله.
